# Mlab-Csharp

```
dotnet tool install --global dotnet-ef --version 3.0.0


dotnet ef dbcontext scaffold "Server=localhost;User Id=root;Password= <password> ;Database=mlab" "Pomelo.EntityFrameworkCore.MySql" -c MyDbContext -o Models

```









